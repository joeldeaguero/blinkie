Setting up a new raspberry pi:
* passwd
* sudo apt update
* sudo apt-get dist-upgrade
* sudo apt-get install python-serial
* Fix keyboard layout
* Enable SSH
* Enable SPI
* Enable I2C
* Enable Serial
